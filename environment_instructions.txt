
##Create virtual environment in vscode terminal
#Download vscode and python 3.12
py -3.12 -m venv .venv (for local)
.\.venv\Scripts\activate (for local)
Set-ExecutionPolicy -ExecutionPolicy Bypass -Scope Process

#Create virtual environment in GCP Cloud Shell and MAC
python3.12 -m venv .venv (for GCP Cloud Shell)
source .venv/bin/activate (for GCP Cloud Shell)
#### ENV ####
GOOGLE_API_KEY=yourkey
GCP_PROJECT_ID=project-b629d2c5-6ec0-4b7d-b32
GCP_REGION=us-central1

poetry add langchain==0.3.27
poetry add python-dotenv==1.1.1
poetry add langchain-google-genai==2.1.12
poetry add numpy==2.2.0
poetry add pandas==2.2.2
poetry add streamlit==1.50.0

# RAG Libraries
poetry add pymupdf==1.25.3 # fitz — PDF text extraction
poetry add langchain-community==0.3.27 # FAISS vectorstore integration (match langchain version)
poetry add faiss-cpu==1.11.0  # FAISS vector similarity search
poetry add langchain-text-splitters==0.3.9 # RecursiveCharacterTextSplitter

## Similarity Evaluation
poetry add nltk==3.9.2
poetry add rouge-score==0.1.2
poetry add bert-score==0.3.13
poetry add sentence-transformers==5.1.2

## RAG Evaluation Libraries
poetry add ragas==0.4.3
poetry add datasets==4.0.0
poetry add deepeval==3.8.9
poetry add google-genai # required for deepeval

## Guardrails
If all above libraries are installed then no need to install any new libraries.



## Onprem Instructions
poetry add langchain-ollama==0.2.0

** Fine Tuning Libraries
poetry add transformers==4.57.3
poetry add peft==0.18.0
poetry add accelerate==1.12.0
poetry add bitsandbytes==0.48.2
poetry add trl==0.25.1
poetry add torch==2.9.1 
    #if you get any error then in pyproject.toml file change requires-python = ">=3.12,<3.15" and install the failed library again


## Agentic Libraries 

** Crewai Libraries
**change below version in pyproject.toml file
    # Before
    "google-genai (>=1.66.0,<2.0.0)",

    # After
    "google-genai (>=1.65.0,<2.0.0)",

poetry add crewai[google-genai]====1.14.1 # incase of any issues change requires-python = ">=3.12,<3.14"

== MCP Libraries for Crew AI ==
poetry add mcp==1.26.0
poetry add fastmcp==2.3.3

** LangGraph Libraries **
If cloud account is created newly then authenticate it using 

gcloud auth login
gcloud auth application-default login
gcloud config set project YOUR_PROJECT_ID
# Enable Vertex AI api
gcloud services enable aiplatform.googleapis.com --project=YOUR_PROJECT_ID

poetry add langgraph==0.3.34

** Langfuse Setup **
Go to https://cloud.langfuse.com/ and create new project and retrieve api keys 
LANGFUSE_SECRET_KEY=YOUR_SECRET_KEY
LANGFUSE_PUBLIC_KEY=YOUR_PUBLIC_KEY
LANGFUSE_HOST=https://cloud.langfuse.com

poetry add langfuse 
poetry add langgraph-checkpoint-sqlite==2.0.6


# Langchain Docs
https://docs.langchain.com/oss/python/langchain/models#google-gemini

## Google models documentation
https://docs.cloud.google.com/vertex-ai/generative-ai/docs/models/gemini/2-5-flash

https://ai.google.dev/gemini-api/docs/embeddings  # Embeddings documentation

### Commands to execute python files in local and GCP
python <yourfile.py>

# To run streamlit file 
streamlit run <file.py>  (for local)

# To run streamlit in Google Cloud Shell
streamlit run 2.llm_with_ui.py \
--server.port 8080 \
--server.address 0.0.0.0 \
--server.enableCORS=false \
--server.enableXsrfProtection=false \
--server.enableWebsocketCompression=false





#FASTAPI :: {# LLM wrapper + landing directory + Pydantic ( base model)}

    
    LLM wrapper:
    
    
    # Vector search : Query vector -> Map to embeddings (ANN:embedding) -> to locate the chunks 
    
    # Context: top-3 chunks
    
    # Prompt 
    # Call LLM
    
    
    # Prompt + LLM
    
    Retrieval + Augmented + Generation(LLM) [ RAG ]
    
    

# Docker + deployment + services

docker image build
create a repo
push the image to the repo



## Deployment

Build a service with Cloud Run.

## Deployment pipeline
   - Triggered based
   - Schedule based



Push to registry -> webhooks/events -> Deployed

Continous Integration -> Continous Deployment :: CI/CD

Script based / pipeline / schdule?event-driven

Vertex pipline /Kubeflow pipeline/ ML flow pipelines :






